gsap.from('#alaska-logo', {
    scrollTrigger: {
      trigger: '#alaska-logo',
    },
    rotate: 360,
    filter: 'blur(10px)',
    scale: 0,
    opacity: 0,
    duration: 2,
    ease: 'expo.out()'
  })
